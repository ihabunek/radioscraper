[project]
name = "radioscraper"
version = "0.1.0"
description = "Collecting radio playlists for no particular reason."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp[speedups]~=3.10",
    "beautifulsoup4~=4.12",
    "django~=6.0",
    "gunicorn~=26.0",
    "psycopg2-binary~=2.9",
    "python-dateutil~=2.9",
    "pytz",
    "sentry-sdk[django]",
    "systemd-python>=235",
]

[dependency-groups]
dev = [
    "django-debug-toolbar",
    "django-extensions~=4.1",
    "django-stubs~=6.0",
    "fabric",
    "factory-boy",
    "ipython",
    "pytest",
    "pytest-django",
]

[tool.ruff]
line-length = 100
